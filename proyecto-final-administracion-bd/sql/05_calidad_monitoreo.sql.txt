-- Tamaño de la base

SELECT pg_size_pretty(pg_database_size('biblioteca_db'));

-- Conexiones activas

SELECT * FROM pg_stat_activity;

-- EXPLAIN

EXPLAIN
SELECT *
FROM prestamos
WHERE id_usuario=3;

-- Correos duplicados

SELECT correo,COUNT(*)
FROM usuarios
GROUP BY correo
HAVING COUNT(*)>1;

-- Libros prestados

SELECT titulo
FROM libros
WHERE disponible=FALSE;